package libs.httparty

// built based on apache httpclient library

object HTTParty {

  var defaultOptions = List("")

  def httpProxy(addr: String, port: Int, user: String, pass: String) = {
  }

  def baseUri(uri: String) = {
  }

  def timeout(time: Int) = {
  }

  def openTimeout(time: Int) = {
  }

  def readTimeout(time: Int) = {
  }

  def headers(header: Map[String, String]) = {
  }

  def cookies(cookie: Map[String, String]) = {
  }

  def followRedirects(flag: Boolean = true) = {
  }

  def format(format: String = "json") = {
  }
}

