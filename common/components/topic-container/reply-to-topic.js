import React, { Component } from 'react';

export default class ReplyToTopic extends Component {

  componentDidMount() {
    if (hasAccessToken) {
    }
  }

  render() {
    return (
      <div>HHH</div>
    );
  }
}